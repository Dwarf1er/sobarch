default:
  filename: "colors/{{ scheme-system }}-{{ scheme-slug }}.jsonc"
  supported-systems: [base16, base24]

tinted8:
  filename: "colors/{{ scheme-system }}-{{ scheme-slug }}.jsonc"
  supported-systems: [tinted8]
  supports:
    tinted8-builder: "~0.2.0"
    tinted8-styling: "~0.2.0"
